services:
  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: pencachi
      POSTGRES_PASSWORD: pencachi
      POSTGRES_DB: pencachi
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
